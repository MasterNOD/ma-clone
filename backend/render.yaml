services:
  - type: web
    name: ma-clone-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ma-clone-db
          property: connectionString

databases:
  - name: ma-clone-db
    plan: free